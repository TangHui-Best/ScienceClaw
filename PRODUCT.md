# Product

## Register

product

## Users

面向需要把浏览器 SOP 转换为可复用自动化技能的业务人员与 RPA 工程人员。用户在录制工作台中同时进行手工浏览器操作和自然语言操作，随后检查步骤、配置参数并编译、测试 Skill。

## Product Purpose

RPA Agent 复用 ScienceClaw 已验证的录制宿主与产品交互，把用户在同一浏览器上下文中的操作沉淀为可解释、可配置、可回放的 Skill。成功标准是用户无需理解底层 Trace、CoreTrace 或编译器实现，也能沿熟悉的录制、配置、测试流程完成可靠自动化。

## Brand Personality

克制、可靠、任务导向。界面应像成熟的生产力工具，优先传达当前浏览器状态、录制步骤和下一步动作，不以装饰或技术术语抢占用户注意力。

## Anti-references

- 不把录制工作台重写为陌生的三栏技术演示页或纯数据模型调试界面。
- 不让 CoreTrace、binding key、candidate/effect 等内部实现概念主导主要交互。
- 不以大面积空白、孤立表单或不一致的按钮体系替代原 ScienceClaw 的连续任务流。
- 不为了视觉新颖性重造浏览器工具栏、对话输入、步骤列表等标准交互。

## Design Principles

1. 旧交互是产品契约：优先复用 ScienceClaw 的页面结构、操作顺序和组件词汇。
2. 新能力渐进出现：新版 RPA Agent 功能通过状态、详情或新增组件补充，不打断原任务流。
3. 事实可见但实现隐身：用户能看到执行进度、步骤和异常，但无需理解后端领域模型。
4. 同一浏览器、同一上下文：手工操作与自然语言操作必须给用户连续、一致的录制体验。
5. 验收以真实任务闭环为准：本地启动、真实 LLM、真实 browser-use 和可编译 Skill 共同构成完成证据。

## Accessibility & Inclusion

保持现有 ScienceClaw 的系统字体与标准控件语义；主要操作具备可见焦点、明确禁用/加载状态和不依赖颜色的状态文案。动效仅用于状态反馈，并尊重减少动态效果偏好。
